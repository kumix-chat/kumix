# plugins (deprecated)

This folder is kept only as a reference for the older layout.

Use `extensions/` for bundled first-party extensions:

- `extensions/ui/*`: iframe UI extensions
- `extensions/proc/*`: worker (proc) extensions

